spring:
  main:
    banner-mode: console

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: none

  devtools:
    livereload:
      enabled: false

  profiles:
    include:
      - secrets

#  logging.level.org.springframework=ERROR

#spring.jpa.hibernate.ddl-auto=none
#spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

logging.level.org.jooq.Constants: ERROR
logging.level.com.filipowm.ambassador.gitlab.ProjectReader: WARN
logging.level.com.filipowm.ambassador.gitlab.ProjectReaderNon: WARN

ambassador:
  indexer:
    concurrency:
      concurrencyLevel: 50